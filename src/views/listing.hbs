<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css"> <!-- Include your custom CSS file -->
    <title>{{listing.name}} - {{user.username}}</title>
    <style>
        /* Custom CSS for carousel background */
        .carousel-container {
            background-color: #f5f5f5; /* Set the background color to grey */
            padding: 20px; /* Add padding for better visual appearance */
        }
    </style>
</head>
<body>
   
    {{> navbar}}
    
    <div class="text-center carousel-container">
    <div class="carousel slide" data-ride="carousel" id="carouselExampleControls"> <!-- Set the ID to match the href attributes -->
        <div class="carousel-inner">
            {{#with listing.photos.[0]}}
            <div class="carousel-item active">
                <img src="data:image/jpeg;base64,{{data}}" alt="Listing Photo" class="card-img-top">
            </div>
            {{/with}}
            
            {{#each listing.photos}}
            {{#if @index}}
            <div class="carousel-item">
                <img src="data:image/jpeg;base64,{{this.data}}" alt="Listing Photo" class="card-img-top">
            </div>    
            {{/if}}
            {{/each}}

        </div>
        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev"> <!-- Ensure href matches the carousel ID -->
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next"> <!-- Ensure href matches the carousel ID -->
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>

    </div>
    <div class="container mt-5 mb-5">
        <div class="row">
            <!-- Listing Details Column -->
            <div class="col-lg-8">
                <h3 class="mb-3">{{listing.name}}</h3>
                <p class="item-price font-weight-bold">PHP {{listing.price}}</p>
                <!-- Listing Details List -->
                <ul class="list-group">
                    <li class="list-group-item"><strong>Condition:</strong> {{listing.condition}}</li>
                    <li class="list-group-item"><strong>Description:</strong> {{listing.description}}</li>
                    <li class="list-group-item"><strong>Category:</strong> {{listing.type}}</li>
                    <!-- Add more list items for other details -->
                </ul>
            </div>
            <!-- Seller Contact Column -->
            <div class="col-lg-4">
                <div class="card">
                    <div class="card-header">Seller Information</div>
                    <div class="card-body">
                        <p><strong>Posted By: </strong><a href="/profile/{{user.username}}" class="text-decoration-none">{{user.username}}</a></p>
                        <p><strong>Email:</strong> {{user.email}}</p>
                        <p><strong>Contact Number:</strong> {{user.phone}}</p>
                        <!-- Add more seller details -->
                    </div>
                </div>
                <!-- Seller Actions -->
            <div class="action-buttons">
                {{#if isCurrentUser}}
                    <button class="btn custom-btn" onclick="window.location.href='/edit-listing/{{listing._id}}'">Edit</button>
                    <form action="/delete-listing/{{listing._id}}" method="POST">
                        <button type="submit" class="btn custom-btn">Delete</button>
                    </form>
                {{/if}}
                <form id="addToCartForm" action="/add-to-cart/{{listing._id}}" method="POST">
                    <button type="submit" class="btn custom-btn">Add to Cart</button>
                </form>
            </div>

        </div>
    </div>

  <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
   
</body>
</html>
