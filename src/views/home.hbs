<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    {{#if isAuthenticated}}
        <h1>Welcome to the Home Page, {{ username }}!</h1>
        <!-- Other authenticated user content -->
        <a href="/logout">Sign Out</a>

        <a href="/post-listing">Post Listing</a>
    {{else}}
        <h1>Welcome to the Home Page! {{ username }}</h1>
        <!-- Other content for non-authenticated users -->
        <a href="/login">Login</a>
        <a href="/signup">Sign Up</a>
    {{/if}}

    {{#if listings.length}}
        <ul>
            {{#each listings}}
                <li>
                    <h2>{{name}}</h2>
                    <p>Brand: {{brand}}</p>
                    <p>Type: {{type}}</p>
                    <p>Price: {{price}}</p>
                    <p>Description: {{description}}</p>
                    {{#if photos.length}}
                        <h3>Photos:</h3>
                        <ul>
                            {{#each photos}}
                                <li><img src="data:image/jpeg;base64,{{this.data}}" alt="Listing Photo"></li>
                            {{/each}}
                        </ul>
                    {{/if}}
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No listings available.</p>
    {{/if}}
</body>
</html>